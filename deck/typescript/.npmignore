# Publish only the built host/ (plus readme, license, package.json).
# base/ is generated by copy:base AND inlined into the bundle by tsup, so
# shipping it would duplicate 13MB of data the runtime never reads.
base/
test/
tmp/
*.config.ts
tsconfig*.json
.npmignore
*.tsbuildinfo
